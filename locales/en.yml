en:
  remarkable:
    mongo_mapper:
      have_key:
        description: "have key(s) {{keys}}"
        expectations:
          has_key: "{{subject_name}} to have key named {{key}} with type {{type}}"